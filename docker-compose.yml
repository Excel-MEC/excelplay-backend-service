version: "3"

services:
  # frontend:
  #   container_name: excelplay-frontend
  #   build:
  #     context: ./excelplay-frontend
  #     dockerfile: dev.dockerfile
  #   volumes:
  #     - ./excelplay-frontend/:/excelplay-frontend
  #     - /excelplay-frontend/node_modules
  #   ports:
  #     - "3000:3000"
  #   restart: on-failure
  #   depends_on:
  #     - kryptos

  kryptos:
    container_name: excelplay-backend-kryptos
    build:
      context: ./excelplay-backend-kryptos/
      dockerfile: prod.dockerfile
    dns:
      - 8.8.8.8
    ports:
      - "8080:8080"
    restart: on-failure
    networks:
      - kryptos_network

  # dalalbull:
  #   container_name: excelplay-backend-dalalbull
  #   build:
  #     context: ./excelplay-backend-dalalbull/
  #     dockerfile: prod.dockerfile
  #   dns:
  #     - 8.8.8.8
  #   ports:
  #     - "8080:8080"
  #   restart: on-failure
  #   networks:
  #     - kryptos_network

  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin_container
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
    ports:
      - "5050:80"
    networks:
      - kryptos_network
    restart: unless-stopped

networks:
  kryptos_network:
    driver: bridge
